name: Prisma Migrate
on: push
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install prisma @prisma/client
      - run: npx prisma migrate deploy --schema=./schema.prisma
        env:
          DATABASE_URL: "postgresql://postgres.rokjbwcnswnlosgrukqb:FARIA2580222@aws-0-eu-north-1.pooler.supabase.com:5432/postgres?sslmode=require"
